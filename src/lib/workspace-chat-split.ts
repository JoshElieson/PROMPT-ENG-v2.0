/** Chat stays primary; secondary panels (terminal, browser, preview, files) cap at half width. */
export const WORKSPACE_CHAT_MIN_RATIO = 0.5;
export const WORKSPACE_SECONDARY_MAX_RATIO = 0.5;
export const WORKSPACE_SECONDARY_MIN_PX = 280;
/** Drag below min width snaps here; release collapses the panel (VS Code–style). */
export const WORKSPACE_SECONDARY_COLLAPSE_THRESHOLD_PX = 120;
